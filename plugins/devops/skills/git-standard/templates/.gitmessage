# <type>(<scope>): <subject>   ← ไม่เกิน ~50 ตัวอักษร / max ~50 chars
#
# ── Body ── (อธิบายว่า "ทำไม" ไม่ใช่ "ทำอะไร" / explain WHY)
#
#
# ── Footer ── (Refs: TICKET-123  หรือ  BREAKING CHANGE: ...)
#
# ───────────────────────────────────────────────────────────
# type: feat | fix | docs | style | refactor | perf | test |
#       build | ci | chore | revert
# subject: คำสั่ง/บอกเล่า, ตัวพิมพ์เล็ก, ไม่ใส่จุดท้าย
#          imperative mood, lowercase, no trailing period
# ───────────────────────────────────────────────────────────
